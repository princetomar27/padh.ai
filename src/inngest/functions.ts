/**
 * Inngest background functions for padh.ai.
 *
 * Phase 2 note: the old meetingsProcessing function (Stream.io + Better Auth)
 * has been removed. Phase 3 will add PDF processing pipeline functions here:
 *   - processBookPdf   — extract pages, render images, create pdfChunks
 *   - generateQuestions — GPT-4o question generation per chapter
 */

// No active functions yet — will be added in Phase 3 (PDF pipeline)
export const inngestFunctions: never[] = [];
