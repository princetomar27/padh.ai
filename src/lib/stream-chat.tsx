/**
 * Stream Chat — removed from padh.ai.
 * padh.ai uses its own session transcript/Q&A system built on top of OpenAI.
 * This stub prevents any lingering imports from breaking the build.
 */
export const streamChat = null;
